{
  "name": "glider-tracker-backend",
  "version": "1.0.0",
  "description": "OGN APRS WebSocket bridge - RAM only, no database",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.17.1",
    "node-fetch": "^3.3.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
